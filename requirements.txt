fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic-settings==2.7.1
librosa==0.10.2
soundfile==0.13.1
numpy>=1.26.4,<2.0
httpx==0.28.1
pytest==8.3.4

# --- WhisperX alignment ---
whisperx==3.1.6
# WhisperX 3.1.6 constructs faster-whisper's TranscriptionOptions with the old
# argument set. faster-whisper >= 1.1.0 made `multilingual` and `hotwords`
# required, so an unpinned resolve pulls a too-new build and every transcribe
# fails with: "TranscriptionOptions.__init__() missing 2 required positional
# arguments: 'multilingual' and 'hotwords'". Pin to the version 3.1.6 targets;
# faster-whisper 1.0.0 pulls a compatible ctranslate2 itself. (If a GPU image
# later hits a cuDNN mismatch, pin ctranslate2==4.4.0 as well.)
faster-whisper==1.0.0
torch>=2.4.0,<2.7
torchaudio>=2.4.0,<2.7
pyannote.audio>=3.1.0
noisereduce==3.0.3
