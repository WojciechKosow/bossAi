/**
 * Feature flags for the app shell.
 *
 * EDITOR_ENABLED — the timeline/project editor is built but parked for V0.2.
 * While false, the editor route redirects to the library and every "Edit"
 * affordance is replaced by a view-only "watch the rendered video" flow.
 * Flip to true to bring the full editor back — no code was removed.
 */
export const EDITOR_ENABLED = false;
