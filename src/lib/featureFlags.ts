/**
 * Google OAuth2 login — not needed for v0.1, hidden on the frontend for now.
 * Flip to true to bring it back; the backend OAuth2 flow is fully
 * implemented and unaffected by this flag.
 */
export const GOOGLE_LOGIN_ENABLED = false;
