/**
 * Title-safe inset: text and UI overlays keep this margin from the frame
 * edges so device bezels, rounded corners and TikTok UI chrome never clip them.
 */
export const TITLE_SAFE_INSET_PERCENT = 5;
export const TITLE_SAFE_INSET = `${TITLE_SAFE_INSET_PERCENT}%`;
